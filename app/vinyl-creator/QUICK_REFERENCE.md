# ⚡ Vinyl Creator - Quick Reference Guide\n\n## 🎯 At a Glance\n\n| Feature | File | Status |\n|---------|------|--------|\n| Export Utilities | `lib/exportUtils.ts` | ✅ Complete |\n| Design Validation | `lib/designValidation.ts` | ✅ Complete |\n| Reconstruction Engine | `lib/ReconstructionEngine.ts` | ✅ Complete |\n| useReconstruction Hook | `hooks/useReconstruction.ts` | ✅ Complete |\n| useReconstructionKeyboard Hook | `hooks/useReconstructionKeyboard.ts` | ✅ Complete |\n| ReconstructionPanel Component | `components/ReconstructionPanel.tsx` | ✅ Complete |\n| Unit Tests | `lib/__tests__/` | ✅ Complete |\n| Component Tests | `components/__tests__/` | ✅ Complete |\n\n---\n\n## 📚 Quick Import Guide\n\n### Components\n```typescript\nimport VinylDesigner from '@/app/vinyl-creator/components/VinylDesigner'\nimport ReconstructionPanel from '@/app/vinyl-creator/components/ReconstructionPanel'\nimport Canvas from '@/app/vinyl-creator/components/Canvas'\nimport DesignPresets from '@/app/vinyl-creator/components/DesignPresets'\nimport ContextMenu from '@/app/vinyl-creator/components/ContextMenu'\nimport AIVinylBuilder from '@/app/vinyl-creator/components/AIVinylBuilder'\n```\n\n### Hooks\n```typescript\nimport { useReconstruction } from '@/app/vinyl-creator/hooks/useReconstruction'\nimport { useReconstructionKeyboard } from '@/app/vinyl-creator/hooks/useReconstructionKeyboard'\n```\n\n### Utilities\n```typescript\nimport { exportAndDownload, exportDesign } from '@/app/vinyl-creator/lib/exportUtils'\nimport { validateDesign, getDesignStats } from '@/app/vinyl-creator/lib/designValidation'\nimport { ReconstructionEngine } from '@/app/vinyl-creator/lib/ReconstructionEngine'\n```\n\n### Types\n```typescript\nimport type { VinylDesign, Shape, ShapeRole, Transform } from '@/app/vinyl-creator/types/vinyl'\n```\n\n### Data\n```typescript\nimport { designExamples } from '@/app/vinyl-creator/data/examples'\nimport { designPresets } from '@/app/vinyl-creator/data/presets'\n```\n\n---\n\n## 🚀 Common Tasks\n\n### Task 1: Export a Design\n```typescript\nimport { exportAndDownload } from '@/app/vinyl-creator/lib/exportUtils'\n\n// Export as JSON\nexportAndDownload(design, 'json')\n\n// Export as SVG\nexportAndDownload(design, 'svg')\n\n// Export as CSV\nexportAndDownload(design, 'csv')\n\n// Export as HTML\nexportAndDownload(design, 'html')\n```\n\n### Task 2: Validate a Design\n```typescript\nimport { validateDesign } from '@/app/vinyl-creator/lib/designValidation'\n\nconst result = validateDesign(design)\nif (result.isValid) {\n  console.log('✓ Design is valid')\n} else {\n  console.error('✗ Errors:', result.errors)\n  console.warn('⚠ Warnings:', result.warnings)\n}\n```\n\n### Task 3: Get Design Statistics\n```typescript\nimport { getDesignStats } from '@/app/vinyl-creator/lib/designValidation'\n\nconst stats = getDesignStats(design)\nconsole.log(`${stats.totalShapes} shapes in ${stats.totalLayers} layers`)\nconsole.log(`${stats.uniqueColors} unique colors`)\nconsole.log(`Average opacity: ${stats.averageOpacity}`)\n```\n\n### Task 4: Use Reconstruction Hook\n```typescript\nimport { useReconstruction } from '@/app/vinyl-creator/hooks/useReconstruction'\n\nconst {\n  currentStep,\n  isPlaying,\n  speed,\n  play,\n  pause,\n  nextStep,\n  previousStep,\n  getProgress\n} = useReconstruction(design)\n```\n\n### Task 5: Add Keyboard Shortcuts\n```typescript\nimport { useReconstructionKeyboard } from '@/app/vinyl-creator/hooks/useReconstructionKeyboard'\n\nuseReconstructionKeyboard({\n  onPlayPause: () => { /* ... */ },\n  onNextStep: () => { /* ... */ },\n  onPreviousStep: () => { /* ... */ },\n  onFirstStep: () => { /* ... */ },\n  onLastStep: () => { /* ... */ },\n  onReset: () => { /* ... */ },\n  onClose: () => { /* ... */ }\n})\n```\n\n### Task 6: Create a Custom Design\n```typescript\nimport type { VinylDesign, Shape } from '@/app/vinyl-creator/types/vinyl'\n\nconst shapes: Shape[] = [\n  {\n    id: 'shape-1',\n    name: 'Base Circle',\n    role: 'base',\n    layer: 0,\n    color: '#FF0000',\n    pathData: 'M 300 250 m -80 0 a 80 80 0 1 0 160 0 a 80 80 0 1 0 -160 0',\n    transform: { x: 0, y: 0, scaleX: 1, scaleY: 1, rotation: 0 },\n    opacity: 1\n  }\n]\n\nconst design: VinylDesign = {\n  id: 'custom-design-1',\n  name: 'My Design',\n  description: 'A custom vinyl design',\n  shapes,\n  buildOrder: ['shape-1'],\n  complexity: 'simple'\n}\n```\n\n### Task 7: Filter Shapes\n```typescript\nimport { findShapesByRole, findShapesByColor } from '@/app/vinyl-creator/lib/designValidation'\n\n// Find all base shapes\nconst baseShapes = findShapesByRole(design.shapes, 'base')\n\n// Find all red shapes\nconst redShapes = findShapesByColor(design.shapes, '#FF0000')\n```\n\n### Task 8: Use Reconstruction Engine Directly\n```typescript\nimport { ReconstructionEngine } from '@/app/vinyl-creator/lib/ReconstructionEngine'\n\nconst engine = new ReconstructionEngine(design)\n\nengine.on('step-change', (stepInfo) => {\n  console.log(`Step ${stepInfo.step} of ${stepInfo.total}`)\n})\n\nengine.play()\nengine.setSpeed(2)\nengine.nextStep()\nengine.destroy() // Always cleanup\n```\n\n---\n\n## 🎮 Keyboard Shortcuts\n\n| Key | Action |\n|-----|--------|\n| `Space` | Play/Pause |\n| `→` | Next step |\n| `←` | Previous step |\n| `Home` | First step |\n| `End` | Last step |\n| `Esc` | Close |\n| `Ctrl+R` / `Cmd+R` | Reset |\n\n---\n\n## 📊 Export Formats\n\n| Format | Use Case | MIME Type |\n|--------|----------|----------|\n| JSON | Data interchange, backup | `application/json` |\n| CSV | Spreadsheet analysis | `text/csv` |\n| SVG | Vector graphics, printing | `image/svg+xml` |\n| HTML | Web viewing, sharing | `text/html` |\n\n---\n\n## 🎨 Shape Roles\n\n| Role | Purpose | Example |\n|------|---------|----------|\n| `base` | Foundation shape | Main circle |\n| `accent` | Secondary shapes | Stripes |\n| `shadow` | Depth/shadow | Dark outline |\n| `highlight` | Light areas | Bright spots |\n| `detail` | Fine details | Small elements |\n\n---\n\n## 📈 Complexity Levels\n\n| Level | Shapes | Use Case |\n|-------|--------|----------|\n| `simple` | 1-10 | Basic designs |\n| `medium` | 11-50 | Standard designs |\n| `complex` | 50+ | Advanced designs |\n\n---\n\n## 🧪 Running Tests\n\n```bash\n# Run all tests\nnpm run test\n\n# Watch mode\nnpm run test:watch\n\n# With coverage\nnpm run test:coverage\n\n# Specific file\nnpm run test -- ReconstructionEngine.test.ts\n```\n\n---\n\n## 🔍 Validation Checks\n\n```typescript\nconst result = validateDesign(design)\n\n// result.isValid: boolean\n// result.errors: string[] - Critical issues\n// result.warnings: string[] - Non-critical issues\n```\n\n**Checks performed:**\n- ✅ All shape IDs exist\n- ✅ Build order references valid shapes\n- ✅ Hex color format validation\n- ✅ Opacity range (0-1)\n- ✅ Layer numbers valid\n- ✅ Design complexity warnings\n- ✅ Large design warnings (100+ shapes)\n\n---\n\n## 📁 File Structure\n\n```\nvinyl-creator/\n├── components/          # UI Components\n├── hooks/              # Custom React Hooks\n├── lib/                # Utilities & Engine\n├── types/              # TypeScript Definitions\n├── data/               # Examples & Presets\n├── page.tsx            # Main Page\n├── README.md           # Implementation Guide\n├── DESIGN_SYSTEM.md    # Design Specification\n├── IMPLEMENTATION_COMPLETE.md\n├── BUILD_SUMMARY.md\n├── FINAL_SUMMARY.md\n├── INTEGRATION_GUIDE.md\n└── QUICK_REFERENCE.md  # This file\n```\n\n---\n\n## 🐛 Common Issues & Solutions\n\n### Issue: Keyboard shortcuts not working\n**Solution**: Ensure hook is called at component level\n```typescript\n// ✓ Correct\nfunction MyComponent() {\n  useReconstructionKeyboard(handlers)\n}\n\n// ✗ Wrong\nfunction MyComponent() {\n  if (condition) {\n    useReconstructionKeyboard(handlers) // Don't do this\n  }\n}\n```\n\n### Issue: Validation errors for valid designs\n**Solution**: Check shape IDs match exactly in buildOrder\n```typescript\n// Make sure all IDs in buildOrder exist in shapes\nconst shapeIds = new Set(design.shapes.map(s => s.id))\ndesign.buildOrder.forEach(id => {\n  if (!shapeIds.has(id)) console.error(`Missing: ${id}`)\n})\n```\n\n### Issue: Export file is empty\n**Solution**: Ensure design has shapes\n```typescript\nif (design.shapes.length === 0) {\n  console.error('Cannot export empty design')\n  return\n}\n```\n\n### Issue: Animation not playing\n**Solution**: Call play() or set autoPlay\n```typescript\n// Option 1: Call play()\nconst { play } = useReconstruction(design)\nplay()\n\n// Option 2: Auto-play\nconst { } = useReconstruction(design, { autoPlay: true })\n```\n\n---\n\n## 💡 Pro Tips\n\n1. **Always validate before exporting**\n   ```typescript\n   const validation = validateDesign(design)\n   if (validation.isValid) exportAndDownload(design, 'json')\n   ```\n\n2. **Use getDesignStats for UI feedback**\n   ```typescript\n   const stats = getDesignStats(design)\n   console.log(`Design: ${stats.totalShapes} shapes, ${stats.uniqueColors} colors`)\n   ```\n\n3. **Cleanup engine resources**\n   ```typescript\n   useEffect(() => {\n     const engine = new ReconstructionEngine(design)\n     return () => engine.destroy()\n   }, [design])\n   ```\n\n4. **Use keyboard shortcuts for better UX**\n   ```typescript\n   useReconstructionKeyboard({\n     onPlayPause: togglePlayPause,\n     onNextStep: handleNext,\n     // ... all handlers\n   })\n   ```\n\n5. **Batch operations with utility functions**\n   ```typescript\n   const baseShapes = findShapesByRole(shapes, 'base')\n   const colors = getUniqueValues(shapes, 'color')\n   ```\n\n---\n\n## 📞 Quick Links\n\n- **Full Documentation**: See `IMPLEMENTATION_COMPLETE.md`\n- **Integration Examples**: See `INTEGRATION_GUIDE.md`\n- **Design System**: See `DESIGN_SYSTEM.md`\n- **API Reference**: See `README.md`\n- **Build Summary**: See `BUILD_SUMMARY.md`\n\n---\n\n## ✨ Feature Checklist\n\n- [x] Export utilities (JSON, CSV, SVG, HTML)\n- [x] Design validation system\n- [x] Reconstruction engine\n- [x] Custom hooks (2)\n- [x] UI components (6)\n- [x] Type definitions\n- [x] Unit tests (15+)\n- [x] Component tests (10+)\n- [x] Keyboard shortcuts (7)\n- [x] Example designs (3)\n- [x] Documentation (5 guides)\n\n---\n\n## 🚀 Next Steps\n\n1. Import components into your pages\n2. Add hooks to your components\n3. Use utilities for validation and export\n4. Run tests to verify everything\n5. Deploy with confidence!\n\n---\n\n**Status**: ✅ COMPLETE  \n**Last Updated**: 2024  \n**Version**: 1.0.0\n"