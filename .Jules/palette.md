## 2026-07-01 - Modal Close Button Accessibility
**Learning:** Icon-only close buttons in modals across the application lacked ARIA labels, making them inaccessible to screen readers.
**Action:** Ensure all icon-only buttons, especially in critical UI components like modals and dialogs, include an `aria-label` for proper screen reader context.
